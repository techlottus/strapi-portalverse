{
  "collectionName": "components_sections_listconfigs",
  "info": {
    "displayName": "listconfig",
    "icon": "tasks",
    "description": ""
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string"
    },
    "maxentries": {
      "default": 12,
      "required": true,
      "type": "integer"
    },
    "relatesto": {
      "type": "enumeration",
      "enum": [
        "blogentries",
        "pages",
        "faq",
        "podcastEpisodes",
      ]
    },
    "sortdate": {
      "type": "enumeration",
      "enum": [
        "latest",
        "earliest"
      ],
      "default": "latest"
    }
  }
}
